name: untitled
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^0.22.0
  font_awesome_flutter: ^9.1.0
  test_api: ^0.3.0
  cupertino_icons: ^1.0.2
  http:
  intl:
  image_picker:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.9.1"

flutter_icons:
  android: true
  ios: true
  image_path: "assets/img/logorp.png"


flutter:


  uses-material-design: true
  assets:
        - assets/img/
